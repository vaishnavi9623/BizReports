services:
  - type: web
    name: laravel-app
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    branch: main
    region: oregon
    buildCommand: ""
    startCommand: php artisan serve --host=0.0.0.0 --port=8000
